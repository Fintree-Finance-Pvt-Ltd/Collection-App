/* 
Original TypeScript interfaces - removed for JS compatibility
Reference only, not used at runtime
*/

 /*
export interface ILmsLoan {
  partnerLoanId: string;
  lan: string;
  customerName: string;
  mobileNumber: string;
  panNumber: string;
  approvedLoanAmount: number;
  emiAmount: number;
  address: string;
  city: string;
  state: string;
  product: string;
  lender: string;
}

export interface ILmsLoanDetails extends ILmsLoan {
  loanAmount: number;
  tenure?: number;
  status?: string;
  interestRate?: number;
  disbursalDate?: string;
}

export interface IProductConfig {
  table: string;
  cols: Record<string, string>; // normalizedField: actualColOrComputed
  manual: {
    table: string;
  };
}

export type ProductKey = keyof typeof import('../../utils').PRODUCT_MAP;
*/

export {};

